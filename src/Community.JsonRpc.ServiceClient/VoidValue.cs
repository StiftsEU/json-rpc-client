namespace Community.JsonRpc.ServiceClient
{
    /// <summary>Specifies a return value type for a service method invocation that does not return a value (a notification).</summary>
    public readonly struct VoidValue
    {
    }
}